import React from "react";
import { View, Text } from "react-native";

const globalStyles = require('../styles/globalStyles');

const ScreenE = () => {
    return (
        <View>
            <Text style={globalStyles.title}>SCREEN E</Text>
        </View>
    )
}

export default ScreenE;